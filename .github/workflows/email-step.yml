name: Email Step Demo

on:
  workflow_dispatch:  # Allows manual triggering

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 24f2002568@ds.study.iitm.ac.in
        run: echo "Hello, this step is named after my email address!"
      
      - name: Show current time
        run: date
